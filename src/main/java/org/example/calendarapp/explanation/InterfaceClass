


    인터페이스가 뭐냐면?

    클래스가 꼭 가져야 할 메서드(기능)들만 정의해두는 틀.
    실제 동작(구현)은 안 하고, "너 이거 반드시 구현해야 해!" 하고 강제하는 역할.
    여러 클래스가 같은 규격을 따르게 할 수 있음.

    Event를 implements 한 클래스들은 반드시 print 메서드를 구현해야함

    구현은 없고, 규격만 정의
    여러 클래스가 공통된 규약을 따르도록 강제할 때 사용