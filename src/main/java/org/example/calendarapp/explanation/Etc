

    @Override
    이 메서드는 부모나 인터페이스 메서드를 재정의한 거다! 라는 표시
    오류 방지용 안전띠
    가독성 = 이 메서드는 내가 새로 만든 게 아니라 “부모/인터페이스 거 재정의한 거구나” 바로 알 수 있음.

    super()
    이건 부모 클래스(상속받은 클래스의 위쪽) 생성자 호출
    항상 자식 생성자 맨 첫 줄에서만 사용 가능
    안 쓰면 자동으로 컴파일러가 super(); 넣어줌 (부모의 기본 생성자 있을 때만)


    클래스 = 설계도
    객체 = 그 설계도로 만든 물건
    객체를 통해 메서드와 데이터를 쓴다

    List → 여러 개의 데이터를 순서대로 담을 수 있는 자료구조(인터페이스)
